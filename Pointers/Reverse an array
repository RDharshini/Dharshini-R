#include <stdio.h>
int main()
{
    int n,*ptr;
    scanf("%d",&n);
    int arr[n];
    for(int i=0;i<n;i++){
        scanf("%d",&arr[i]);
    }
    ptr=arr;
    for(int i=n-1;i>=0;i--){
        printf("%d ",*(ptr+i));
    }
    return 0;
}
