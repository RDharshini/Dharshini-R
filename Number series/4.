#include <stdio.h>

int main()
{
    int n;
    scanf("%d",&n);
    int a=8;
    printf("%d ",a);
    int c=1;
    for(int i=0;i<n;i++){
        a=(2*a)-c;
        printf("%d ",a);
        c++;
    }

    return 0;
}
